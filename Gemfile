# frozen_string_literal: true

source "https://rubygems.org"

repo_name = 'rleber/<%= repo %>.git'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'rake'
gem 'shellwords'
gem 'httparty'
